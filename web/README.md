# Personal Task Manager Web

React + Vite frontend for the task manager API.

## Scripts

- `npm run dev` starts the web dev server with `/api` proxied to `http://localhost:4000`.
- `npm run build` creates the production build in `dist`.
- `npm run dev:server` starts the API from `../server`.
